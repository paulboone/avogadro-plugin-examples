This repository holds a template you can use to create your own python plugins for avogadro.

Requires Python 3.5+

To test:

```
pip install -r requirements.txt

./install
./run --help
./run --print-dialog < examples/ethane.json
./run < examples/ethane.json
```
